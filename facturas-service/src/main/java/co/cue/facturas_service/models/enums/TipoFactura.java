package co.cue.facturas_service.models.enums;
// Enum que representa los diferentes tipos de facturas que puede manejar el sistema.
public enum TipoFactura {
    // Factura generada por la compra o venta de productos.
    PRODUCTOS,

    // Factura generada por la prestación de un servicio de cita (consulta, vacunación, etc.).
    CITA
}
